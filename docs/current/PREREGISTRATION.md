# Pre-registration — The Action-Interface Study

> **STATUS: DRAFT.** Written 2026-09-19, before any study data was collected.
> Everything marked **TBD** is resolved after the Phase 2 pilot, at which point this
> document is **frozen** and the commit tagged `study-freeze`. No hypothesis, metric,
> exclusion rule or analysis choice below may change after that tag — later changes
> are recorded as clearly-labelled deviations in §10, never by editing the original.
>
> Branch at time of writing: `feat/interface-study`, from `main` at `da0b7b7`.

**Why this document exists.** Once results exist, there are many individually
defensible choices — drop a scenario that misbehaved, report eventual instead of
first-attempt validity, exclude the seeds where the model crashed — that collectively
allow a finding to be manufactured from noise. Fixing those choices in advance, in a
commit that demonstrably predates the data, is what separates a study from a set of
runs. It also lets a *null* tactical result be reported as a finding (H3) rather than
as a failed search.

---

## 1. Research question

How do **free-text**, **schema-constrained** and **engine-enumerated** action
interfaces affect an LLM agent's **first-attempt action validity**, **recovery**,
**cost** and **tactical outcome** in a deterministic tactical environment?

Secondary:
- Does constraint improve tactics, or only formatting and legality?
- Which invalid-action categories survive each level of constraint?
- What does validator feedback buy in recovered actions, and at what token cost?
- **Does enumeration cost expressivity?** A menu discretises movement and AoE aim
  points, so it can remove good options. See §3 (H4).

**Relation to prior work.** D20Bench *uses* a legal-action catalogue as its contract;
this study **ablates** that choice. Related work to cite (each verified before freeze):
D20Bench; *Setting the DC: Tool-Grounded D&D Simulations to Test LLM Agents*;
GameBench (arXiv 2406.06613); SmartPlay (arXiv 2310.01557); *Let Me Speak Freely?*
(Tam et al., 2024) on format-restriction effects; invalid-action masking in RL
(Huang & Ontañón, 2020).

---

## 2. Conditions

Four conditions. **The state observation is byte-identical across all four** — same
information policy, same JSON body. Only the *action section* of the prompt and the
response channel differ. Every prompt variant is hashed and the hash recorded.

| | **C1 FREE** | **C2 SCHEMA** | **C2+M** | **C3 MENU** |
|---|---|---|---|---|
| Output | Plain text in a declared grammar | Native tool call, raw params | Native tool call, raw params | One tool: `choose(action_id)` |
| Legal menu shown? | No | No | **Yes** | **Yes** |
| Parser | Deterministic grammar parser | Provider tool-calling + engine validation | Same as C2 | ID lookup + engine revalidation |
| Arbitrary points expressible? | Yes | Yes | Yes | No — discretised candidates |

**C2+M is included as a full condition, not a pilot extra.** C3 moves two dials at
once: output *format* and *affordance* (being told what is legal). Without C2+M, a
C2/C3 difference has two candidate causes and cannot be attributed to either.
C2 → C2+M isolates affordance; C2+M → C3 isolates format. It is also the cheapest
cell to build, being close to the existing production path.

**Constraints on the menu (C3):**
- Candidates are generated by a **neutral rule**, never by `HeuristicAgent`'s search.
  Otherwise the menu smuggles in the heuristic's intelligence and inflates C3.
- AoE aim points are produced by sweeping a moderate-resolution grid and keeping one
  representative per **distinct set of targets hit** — there is never a reason to
  offer two points that hit the same creatures.
- Menu length is capped, deterministically ordered, and **recorded per decision** as a
  cost covariate.
- Every listed `action_id` must execute successfully (enforced by a property test).

**Important:** menu discretisation is an *interface* affordance only. The engine
remains continuous underneath — SRD 5.1 measures in feet and has no grid rules (the
5-ft square is the PHB's "Variant: Playing on a Grid", absent from the SRD).

**C1 fairness:** the grammar plus 2–3 worked examples appear in the prompt. A parse
failure is the `malformed_output` category. The parser is frozen before the pilot and
covered by adversarial and near-miss tests. It is **never** an LLM parser — that would
introduce a second model into the measurement.

---

## 3. Hypotheses

Directional predictions, registered before data collection.

- **H1 — Validity.** First-attempt valid-action rate: **C3 > C2+M > C2 > C1.**
- **H2 — Locus.** The C1/C2 validity deficit concentrates in **spatial** actions
  (move, AoE aim point) rather than attack or end-turn.
- **H3 — Constraint buys legality more than skill.** The between-condition gap in
  *tactical* score is **smaller** than the gap in *validity*. A null tactical result
  supports H3; it is not a failed search.
- **H4 — Expressivity cost (directional, weak).** On the AoE scenario, C3's best
  available AoE placement is **no better, and possibly worse**, than the best
  placement C2/C2+M can express freely, because C3 can only choose from discretised
  aim points. Measured as realised AoE targets-hit per cast.

**Exploratory, no direction predicted:** tokens and dollars per *accepted* action
(C3 has a longer input menu but fewer retries); latency; whether effects depend on
model capability.

---

## 4. Held fixed

| Factor | Value |
|---|---|
| Scenarios | `kiting`, `alpha_strike`, `protect_squishy`, **+ one new AoE scenario** (§4.1) |
| Opponent | A single fixed non-LLM agent, **TBD (Scripted vs Heuristic), chosen in the pilot** and identical across all cells thereafter |
| LLM side | The scenario's `llm_team` |
| Information policy | One fixed policy: the current default (all enemy info revealed, `hp_display="exact"`). Information sensitivity is a different study. |
| Temperature | 0 (or provider minimum), recorded. Still not deterministic — stated as a limitation. |
| Failure budget | Existing: 3 consecutive **or** 5 total failed calls per turn → turn forced to end. Rejection feedback returned to the agent. |
| Round cap | 20 (`DEFAULT_ROUND_CAP`) |
| Seeds | 10 per cell, **paired** across conditions |
| Engine | Pinned commit, recorded in every match manifest |

### 4.1 The AoE scenario (to be built in Phase 1)

A 2v2 including a caster with an area spell on the LLM side. Required because **no
existing scenario uses any spell at all** — `src/arena/scenarios.py` is weapon-attacks
only, so without it movement is the sole discretised axis and H4 cannot be tested.
This is the first arena scenario needing spell-registry wiring.

It must be **skill-revealing but not a puzzle**: a good placement should hit two
enemies while sparing an ally, so that both correct targeting and the expressivity
cost of a discretised menu are observable.

---

## 5. Models

Run through **OpenRouter for every model**, including Claude. Mixed routing (direct
Anthropic API for one model, OpenRouter for others) would introduce a provider-path
confound for the sake of spending existing credits; that trade is declined
deliberately.

| Model | Role | Status |
|---|---|---|
| Nemotron 3.5 | Cheap/accessible baseline | **Confirmed.** A paid host is used for throughput; "free-tier model" describes availability, not how it is run. |
| Gemini 3.8 | Fast, competitive mid-tier | **Confirmed** |
| Claude Sonnet | High-capability reference | **Conditional on budget**, decided after observing the first two models' realised token cost. Currently discounted (~$0.75/$3.75 per M tokens). |

Exact model strings are pinned before the final run, and the **provider-returned
model id** is recorded per call. Tool-calling support is confirmed in preflight for
every model — C2, C2+M and C3 all require it; a model without it could only run C1
and would leave a hole in the grid.

**Recorded, not assumed:** early Nemotron testing showed very large token churn
(~700k tokens across three fights), substantially from repeated illegal calls before
the failure budget forced the turn to end. That churn is **itself a dependent
variable** (cost per accepted action) and is expected to be worst in C1. It is
therefore a result to report, not a defect to suppress — but it does set the budget
envelope, so the runner enforces a hard spend cap.

---

## 6. Metrics

**Primary:** first-attempt valid-action rate, per decision.

**Invalid-action taxonomy** (typed codes emitted by `ToolExecutor`, one test per code):
`malformed_output`, `unknown_action`, `unknown_target`, `invalid_target_relation`,
`out_of_range`, `destination_blocked`, `insufficient_resource`,
`action_economy_spent`, `no_tool_call`.

**Secondary:**
- Recovery rate: P(valid on next attempt | rejected)
- Forfeit turns (failure budget exhausted)
- Cost: input/output tokens per decision and per **accepted** action, latency, $
- Tactics: scenario objective score (`kiting_adherence`, `protected_survival`, …),
  HP margin, win rate
- AoE realised targets-hit per cast (H4)
- Menu length (C3/C2+M) as a cost covariate

**Harness check, not a result:** replay verification rate, which must be 100%.

---

## 7. Analysis

- Decisions within a match are **not independent**. Per-decision rates use a
  **cluster (match-level) bootstrap**; win rates use **Wilson** intervals. **No Elo.**
- Seeds are **paired** across conditions (same scenario and seed in every cell). LLM
  nondeterminism breaks pairing after the first differing decision; this is stated as
  a limitation rather than corrected for.
- **Baselines** (Random, Scripted, Heuristic) run through the **C3 path** using the
  same executor, to anchor tactical metrics. They cost no API calls.

### 7.1 Sizing

4 conditions × 4 scenarios × 10 seeds = **160 matches per model**.

At the ~35 LLM calls/match observed in the 2026-09-15 diagnostic, that is
**~5,600 calls per model**; ~11,200 for the two confirmed models, plus ~5,600 more if
Claude is included. Adding the AoE scenario increased this by a third over the
original 3-scenario plan — accepted, because H4 is otherwise untestable.

Validity effects are large with hundreds of decisions per cell, so 10 seeds is ample
there. **Tactical differences will be underpowered.** That is anticipated, and is why
H3 is framed around the *relative* size of the validity and tactics gaps rather than
around detecting a tactical effect.

---

## 8. Exclusions

A match is excluded **only** for provider or infrastructure failure — HTTP 5xx,
rate-limit abort, malformed provider response, model unavailability. **Never for bad
model behaviour**: illegal actions, forfeited turns, losses and nonsense output are
data, not errors. Excluded seeds are re-run and the exclusion count is reported.

---

## 9. Threats to validity

Declared in advance: a single environment; few models; the format/affordance confound
(mitigated but not eliminated by C2+M); menu discretisation; LLM nondeterminism at
temperature 0; underpowered tactical comparisons; the neutral-prompt choice; and the
fact that the same author wrote both the engine and the interfaces under test.

---

## 10. Deviations from this pre-registration

*(None yet. After the `study-freeze` tag, every departure is recorded here with its
date and reason. Entries are appended, never edited.)*

---

## 11. Open items before freeze

- [ ] Opponent: Scripted vs Heuristic, decided in the pilot
- [ ] Whether Claude Sonnet is in the final run (budget, after two models' real cost)
- [ ] Exact pinned model strings
- [ ] Final AoE scenario definition
- [ ] Prompt texts and their hashes for all four conditions
- [ ] Confirmed per-match call and token counts from the pilot
- [ ] Citation verification for every work listed in §1
